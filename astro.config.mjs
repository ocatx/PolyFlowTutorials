import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://ocatx.github.io',   // ← Change to your username
  base: '/',                                // Important for username.github.io
});
