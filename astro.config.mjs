import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://ocatx.github.io',   // ← Change to your username
  base: '/PolyFlowTutorials',                                // Important for username.github.io
});
